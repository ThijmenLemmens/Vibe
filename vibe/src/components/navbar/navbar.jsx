import {useEffect} from "react";

export const Navbar = (props) => {

    useEffect(() => {

    }, []);

    return (
        <header className="flex w-full p-2 mb-2 border">
            <p className="p-1 border border-amber-300">Vibe</p>
        </header>
    );
}